<li>
	{{burger_name}}
	
	<button class="change-devoured btn btn-light" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}DEVOUR THIS!{{else}}Make again!{{/if}}
	</button>

	<button class="delete-burger btn btn-light" data-id="{{this.id}}">DELETE!</button>
</li>